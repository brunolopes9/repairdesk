# Placeholder para garantir que a pasta existe (bind mount do docker-compose.yml).
#
# Colocar aqui os ficheiros do certificado AT para o webservice DadosTOI/NIF lookup:
#   - ChaveCifraPublicaAT2027.cer  (certificado público)
#   - at-private-key.pem           (chave privada)
#
# NUNCA commitar os ficheiros reais — estão ignorados via .gitignore (*.pem, *.key, *.cer).
# Em produção, os ficheiros devem estar em /opt/repairdesk/secrets/at/ (ver .env.production.example).
